
/// The `f` namespace contains all the functions that would otherwise be *free*. This is usef.ul for two reasons:
/// - by typing `f.` Xcode will autocomplete with all the available functions;
/// - Swift idiomatically enforces namespacing: most of the free functions in the past have been turned into methods;

public enum f {}
